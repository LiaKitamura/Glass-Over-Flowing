<h1>Admin Dashboard</h1>

<ul>
  <li><%= link_to 'Manage Beers', admin_beers_path %></li>
  <% if user_signed_in? %>
    <% if current_user.admin? %>
      <li><%= link_to 'See page as user', user_session_path %></li>
      <% end %>
  <% end %>
</ul>
